# Frontend Environment Variables (Optional)
# The frontend uses proxy configuration in vite.config.ts by default

# If you want to override the API URL:
# VITE_API_URL=http://localhost:8000

# Note: You don't need Azure OpenAI keys here!
# Those go in the backend .env file (project root)
